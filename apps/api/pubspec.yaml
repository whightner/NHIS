name: nhis_api
description: Dart API workspace for the NHIS platform.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ^3.7.0

dependencies:
  crypto: ^3.0.7
  postgres: ^3.5.7
  shelf: ^1.4.0
  shelf_router: ^1.0.0
  dart_jsonwebtoken: ^2.3.0
  bcrypt: ^1.2.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
