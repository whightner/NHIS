name: nhis_api
description: Dart API workspace for the NHIS platform.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.7.0

dependencies:
  postgres: ^3.5.7

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
